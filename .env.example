# Stripe API Keys (get from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Resend API Key (get from https://resend.com/api-keys)
RESEND_API_KEY=re_your_resend_api_key

# Email sender (must be verified domain in Resend, or use onboarding@resend.dev for testing)
FROM_EMAIL=onboarding@resend.dev

# PDF attachment (base64 encoded) - optional, for small PDFs
# You can encode your PDF: cat yourfile.pdf | base64
# PDF_BASE64=JVBERi0xLjQKJeLjz9...

# Site URL (auto-set by Netlify in production)
# URL=https://your-site.netlify.app
